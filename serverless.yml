service: writingCheck

frameworkVersion: ">=1.1.0 <2.0.0"

provider:
  name: aws
  runtime: nodejs8.10

functions:
  writingCheck:
    handler: handler.endpoint
    events:
      - http:
          path: check
          method: post
          cors: true
